<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="com.thewizrd.simpleweather.controls.graphs.RangeBarGraphEntry" />

        <variable
            name="data"
            type="RangeBarGraphEntry" />
    </data>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingHorizontal="4dp"
        android:orientation="vertical"
        android:gravity="bottom"
        tools:layout_width="70dp"
        tools:layout_height="250dp">

        <LinearLayout
            android:id="@+id/inner_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_gravity="center_horizontal"
            android:gravity="bottom"
            android:orientation="vertical">

            <TextView
                android:id="@+id/bar_hi"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:maxLines="1"
                android:padding="4dp"
                android:text="@{data.hiTempData.label}"
                tools:text="89°" />

            <com.thewizrd.simpleweather.controls.GradientBar
                android:id="@+id/rangebar"
                android:layout_width="6dp"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:layout_gravity="center_horizontal" />

            <TextView
                android:id="@+id/bar_lo"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:maxLines="1"
                android:padding="4dp"
                android:text="@{data.loTempData.label}"
                tools:text="66°" />

        </LinearLayout>

        <androidx.legacy.widget.Space
            android:layout_width="wrap_content"
            android:layout_height="8dp" />

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/bar_icon"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="center_horizontal"
            android:scaleType="fitEnd"
            android:rotation="@{data.XIconRotation}"
            app:srcCompat="@{data.XIcon}"
            tools:srcCompat="@drawable/wi_day_sunny" />

        <TextView
            android:id="@+id/bar_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:gravity="center"
            android:maxLines="1"
            android:padding="4dp"
            android:text="@{data.XLabel}"
            tools:text="Sat 10" />

    </LinearLayout>
</layout>